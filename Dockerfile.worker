FROM node:20-alpine

WORKDIR /app

# Install dependencies first for better layer caching
COPY package*.json ./
RUN npm ci

# Copy source code
COPY . .

# Prisma client is generated via postinstall, but regenerate defensively
RUN npx prisma generate

ENV NODE_ENV=production
CMD ["npm", "run", "worker"]
